<%- include("partials/header.ejs") %>
    <div class="container flex">
        <div class="grid-wrapper">
            <div class="head">Work Tasks:</div>
            <% if (locals.workToDo && workToDo.length > 0) { %>
                <% for (let i = 0; i < workToDo.length; i++) { %>
                    <div class="grid-item">
                        <%= workToDo[i].name %>
                    </div>
                <% } %>
            <% } else { %>
                <div class="grid-item">No tasks added!</div>
            <% } %>
        </div>
    </div>
<%- include("partials/footer.ejs") %>