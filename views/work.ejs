<%- include("partials/header.ejs") %>
    <div class="container flex">
        <div class="grid-wrapper">
            <div class="head">Work Tasks:</div>
            <% if (locals.workToDo && workToDo.length > 0) { %>
                <% for (let i = 0; i < workToDo.length; i++) { %>
                    <form method="post" action="/delete" id="myForm">
                        <input name="checkbox" type="checkbox" id="myCheckox" value="<%=workToDo[i]._id%>">
                        <button class="grid-item" type="submit" id="myButton"><%= workToDo[i].name %></button>
                    </form>
                <% } %>
            <% } else { %>
                <div class="grid-item">No tasks added!</div>
            <% } %>
        </div>
    </div>
<%- include("partials/footer.ejs") %>