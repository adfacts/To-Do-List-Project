<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ToDo List</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
    <link href="styles/main.css" rel="stylesheet" />
    
</head>
<body class="color">
    <nav class="navbar navbar-expand-lg bg-body-tertiary">
        <div class="container-fluid">
          <a class="navbar-brand" href="#"><strong>ToDo List</strong></a>
          <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
              <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="/"><strong>All Tasks</strong></a>
              </li>
              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                  <strong>Lists</strong>
                </a>
                <ul class="dropdown-menu">
                  <% if (locals.customLists && customLists.length > 0) { %>
                    <% for (let i = 0; i < customLists.length; i++) { %>
                      <li>
                        <a class="dropdown-item" href="<%= "/" + customLists[i].name %>"><%= customLists[i].name %></a>
                      </li>
                    <% } %>
                  <% } else { %>
                    <li><a class="dropdown-item" href="#">No Lists created</a></li>
                  <% } %>
                </ul>
              </li>
            </ul>
              <form action="/createList" method="post" class="d-flex" role="input">
                <input type="text" class="form-control me-2" name="customListName" required placeholder="Create New List" aria-label="Create New List">
                <button class="btn btn-outline-success" type="submit">Create</button>
              </form>
          </div>
        </div>
      </nav>
</body>
</html>